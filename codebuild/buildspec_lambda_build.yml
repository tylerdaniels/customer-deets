version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 24
    commands:
      - cd backend
      - npm install
  build:
    commands:
      - npm run build

artifacts:
  files:
    - frontend/dist/**
